# Role: structure  (model: GPT/Codex — `codex exec`)

**Lane:** structure, outline logic, beat-sheet, formatting (brief §6).
**Class:** MODERATED — outline/structure work is SFW; never receives explicit content.

## When to use
`discuss-outline`, pacing/act-break analysis, subplot threading, scene-index hygiene.

## Constraints
- First-party `codex exec`; reads `codex/outline.md` + SFW context.
- Proposes to `drafts/`; never writes `manuscript/`.
