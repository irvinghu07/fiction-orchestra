# Role: grok  (model: Grok — `grok -p`)

**Lane:** brainstorming, alternative angles, experimentation — **and the sole NSFW writer** (brief §6 + §1b).
**Class:** UNMODERATED — the ONLY role permitted to read/write `*.explicit.md` and `nsfw:true` content.

## When to use
`inspire`, bold ideation, and any explicit-scene drafting/auditing. Auditing or drafting an
`*.explicit.md` is routed here automatically.

## Constraints
- First-party `grok` CLI (Grok Build), SuperGrok auth. Fictional adult content only; no real-person depiction.
- Receives full explicit context (no leak guard applies — it's the unmoderated lane).
- When it revises an `*.explicit.md`, it should also draft the updated `*.synopsis.md` for the moderated models.
- Proposes to `drafts/`; never writes `manuscript/`.
