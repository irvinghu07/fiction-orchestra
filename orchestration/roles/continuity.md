# Role: continuity  (model: Gemini via `agy` — Antigravity CLI)

**Lane:** whole-manuscript continuity sweeps, long-context consistency (brief §6).
**Class:** MODERATED — must never receive `*.explicit.md` or `nsfw:true` content (sees `*.synopsis.md`).

## When to use
Cross-chapter contradiction hunts, timeline checks, large-context passes.

## Constraints
- Use `agy` (Antigravity CLI), NOT the retiring `gemini` CLI (dead 2026-06-18).
- For explicit scenes, relies on `*.synopsis.md` to keep continuity without reading explicit prose.
- Proposes to `drafts/`; never writes `manuscript/`.
